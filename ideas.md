### Things to categorize by-
- Declarations
- Keywords (if, for, repeat etc.)
- Operators
- Identifiers (variables, functions, classes etc.)
- Data types
    Integer
    Real
    Char
    String
    Boolean
    Date
    Array
- Comments & whitespace


### Remaining things
I think the base has been done. After this, I need to include usage of all datatypes including- real, char, string, boolean. 
I think the parser should first have just assignments such as "x <- 3" as the inclusion of things such as loops is going to be complicated. I also need to write a program which will take the .pseudo or whatever the file is named and translate it into raw text.
I did some further research, and found that I can just write it into a text file, give the name to python (which will read it and save all the characters itself), and let python read it and save it as a string. I don't have plans of making it something other than a string.
After that, I also need to add the instructions of the pseudocode itself, such as the command "DECLARE x : INTEGER" declaring the variable x to be an integer. 